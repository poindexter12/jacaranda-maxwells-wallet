# Secrets configuration (copy to .envrc.secrets and fill in values)
# This file is gitignored - never commit secrets

# 1Password Connect (optional - for CI/CD and team environments)
# export OP_CONNECT_HOST="http://secrets-server.mgmt:8080"
# export OP_CONNECT_TOKEN="your-connect-token-here"

# Alternative: 1Password CLI service account
# export OP_SERVICE_ACCOUNT_TOKEN="your-service-account-token"
