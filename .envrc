# direnv configuration for jacaranda-maxwells-wallet
# Activates mise + Python venv when entering directory

# Load mise environment
use mise

# Activate Python venv if it exists
if [ -d .venv ]; then
  source .venv/bin/activate
fi

# Load secrets if present (gitignored)
if [ -f .envrc.secrets ]; then
  source_env .envrc.secrets
fi
